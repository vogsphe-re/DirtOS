# Handle all text files automatically
* text=auto

# Force specific files to use CRLF (e.g., Windows batch files)
*.bat text eol=crlf

# Force specific files to use LF (e.g., shell scripts)
*.sh text eol=lf